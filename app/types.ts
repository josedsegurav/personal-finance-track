export interface Supabase {
    data: Array<Object>
    error: string;
}

export interface Category {
    id: number;
    category_name: string;
}

export interface Store {
    id: number;
    store_name: string;
}

